import React from "react";
import "../style/Header.css";

function Header() {
    return (
    <header>
        <h1 className="header">User Directory</h1>
        <h3 className="header">Manage your user's information with the directory application. You can search for a user by name:</h3>
    </header>
    );
};

export default Header;